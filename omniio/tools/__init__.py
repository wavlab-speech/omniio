"""Format-specific helpers that are not tied to omniio's own archive layout.

Each subpackage here is re-exported at the top level (``omniio.kaldi``), so
importers do not need to know where the code lives; see :mod:`omniio`.
"""
